{
  "name": "wheresmymoney-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "start": "docker compose up --build",
    "stop": "docker compose down",
    "logs": "docker compose logs -f"
  }
}
